---
- name: Converge
  hosts: all
  become: true
  vars_files:
    - vars/test-vars.yml
  pre_tasks:
    - name: Create Docker config directory
      ansible.builtin.file:
        path: /etc/docker
        state: directory
        mode: "0755"
    - name: Configure Docker to use vfs storage driver
      ansible.builtin.copy:
        content: |
          {
            "storage-driver": "vfs"
          }
        dest: /etc/docker/daemon.json
        mode: "0644"
    - name: Install Docker prerequisites
      ansible.builtin.apt:
        name:
          - ca-certificates
          - curl
          - gnupg
        state: present
        update_cache: true
    - name: Create keyrings directory
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        mode: "0755"
    - name: Download Docker GPG key
      ansible.builtin.get_url:
        url: "https://download.docker.com/linux/{{ ansible_distribution | lower }}/gpg"
        dest: /etc/apt/keyrings/docker.asc
        mode: "0644"
    - name: Add Docker repository
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc]
          https://download.docker.com/linux/{{ ansible_distribution | lower }}
          {{ ansible_distribution_release }} stable
        filename: docker
        state: present
    - name: Install Docker packages
      ansible.builtin.apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-compose-plugin
          - python3-docker
        state: present
        update_cache: true
    - name: Ensure Docker service is started
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: true
    - name: Pre-create secrets directory for password lookup
      ansible.builtin.file:
        path: /opt/paperless/secrets
        state: directory
        mode: "0700"
        owner: root
        group: root
    - name: Pre-create db_password file for password lookup
      ansible.builtin.copy:
        content: "testdbpassword"
        dest: /opt/paperless/secrets/db_password
        mode: "0600"
        owner: root
        group: root
        force: false
    - name: Pre-create secret_key file for password lookup
      ansible.builtin.copy:
        content: "testsecretkey"
        dest: /opt/paperless/secrets/secret_key
        mode: "0600"
        owner: root
        group: root
        force: false
  roles:
    - role: paperless
