---
- name: Converge
  hosts: all
  become: true
  vars_files:
    - vars/test-vars.yml
  pre_tasks:
    - name: Install Git for test environment
      ansible.builtin.apt:
        name: git
        state: present
        update_cache: true
  roles:
    - role: dify
